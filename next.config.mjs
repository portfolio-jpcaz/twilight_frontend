/** @type {import('next').NextConfig} */
const nextConfig = {
  devIndicators: {
    buildWatermark: false,
  },
  devIndicators: false,
   // Add this to verify config is working
   reactStrictMode: false,
};

export default nextConfig;
